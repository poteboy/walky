type Query {
  users: [User]
}

type Mutation {
  registerUser(uid: ID!, name: String!, phone: String!): User
}

type User {
  uid: ID!
  name: String!
  phone: String!
}

schema {
  query: Query
  mutation: Mutation
}
